apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springuma-chart.fullname" . }}-test-connection"
  labels:
    {{- include "springuma-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "springuma-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
